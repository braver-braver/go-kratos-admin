data:
  database:
    driver: "postgres"
    source: "host=timescaledb.pg15-timescale.orb.local port=5432 user=postgres password=*Abcd123456 dbname=kratos_admin sslmode=disable"
    migrate: true
    debug: false
    enable_trace: false
    enable_metrics: false
    max_idle_connections: 25
    max_open_connections: 25
    connection_max_lifetime: 300s

  redis:
    addr: "redis-master.redis-master-slave.orb.local:6379"
    password: ""
    dial_timeout: 10s
    read_timeout: 0.4s
    write_timeout: 0.6s
